name: water_jug_riddle
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # ui packages
  animated_widgets: ^1.1.0
  diffutil_sliverlist: ^0.5.0
  cupertino_icons: ^1.0.3


  # state management packages
  provider: ^6.0.0
  mobx: ^2.0.0
  flutter_mobx: ^2.0.0
  dart_json_mapper: ^2.0.6
  dart_json_mapper_mobx: ^2.0.0


  # audio package
  audioplayers: ^0.19.1


  # localization packages
  flutter_localizations:
    sdk: flutter
  flutter_cupertino_localizations: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # self-made package for generating files&string resources
  flutter_serve:
    path: ../flutter_serve

  # state management packages
  build_runner: ^2.1.1
  mobx_codegen: ^2.0.0


  flutter_lints: ^1.0.4

flutter:


  assets:
    - assets/languages/
  uses-material-design: true


flutter_serve:
  languageFile: "languages/en_US.json"
  languageFolder: "languages"