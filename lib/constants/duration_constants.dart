abstract class ConstantsDuration {
  static const Duration shakeButton = Duration(milliseconds: 1500);
  static const Duration animatedSwitcherDefault = Duration(milliseconds: 300);
  static const Duration sliverListElementFadeDefault = Duration(milliseconds: 300);
  static const Duration sliverListElementPauseDefault = Duration(milliseconds: 300);
}
